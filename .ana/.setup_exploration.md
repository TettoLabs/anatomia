# Exploration Results

_Generated: 2026-03-23T14:44:00Z_

## Project Identity

- **Name:** anatomia-workspace (from /Users/rsmith/Projects/anatomia_project/anatomia/package.json) — Confidence: 0.95
- **CLI Package Name:** anatomia-cli (from /Users/rsmith/Projects/anatomia_project/anatomia/packages/cli/package.json) — Confidence: 0.95
- **Analyzer Package Name:** anatomia-analyzer (from /Users/rsmith/Projects/anatomia_project/anatomia/packages/analyzer/package.json) — Confidence: 0.95
- **Description:** "Anatomia CLI development workspace" (root), "AI context framework with analyzer-driven setup" (CLI), "Code analysis engine for Anatomia" (analyzer) — Confidence: 0.95
- **Version:** 0.2.0 (CLI), 0.1.0 (analyzer), 0.0.0 (workspace root) — Confidence: 0.95
- **License:** MIT — Confidence: 0.95
- **Entry point:** packages/cli/src/index.ts (CLI entry with #!/usr/bin/env node shebang) — Confidence: 0.90
- **Binary:** ana (packages/cli/package.json bin field) — Confidence: 0.95
- **Repository:** https://github.com/TettoLabs/anatomia — Confidence: 0.95
- **Author/Organization:** TettoLabs — Confidence: 0.95

## Framework and Language

- **Primary language:** TypeScript — Confidence: 0.95
  - Evidence: tsconfig.base.json, tsconfig.json in all packages, all source files are .ts
  - TypeScript 5.7.0 in devDependencies
  - Strict mode enabled with extensive strict checks
- **Runtime:** Node.js 20+ (from engines field in package.json) — Confidence: 0.95
- **Module system:** ESM (type: "module" in all package.json files, import/export syntax) — Confidence: 0.95
- **Monorepo structure:** Turborepo + pnpm workspaces — Confidence: 0.95
  - Evidence: turbo.json, pnpm-workspace.yaml, packages/ directory
  - Workspace packages: cli, analyzer, generator
- **Build tools:**
  - CLI: tsup (ESM bundler for Node) — Confidence: 0.95
  - Analyzer: tsc (TypeScript compiler) — Confidence: 0.95
  - Turborepo for monorepo orchestration — Confidence: 0.95
- **Package manager:** pnpm 9.0.0 (from packageManager field) — Confidence: 0.95
- **Framework/Libraries:**
  - CLI: commander (12.0.0), chalk (5.3.0), ora (8.0.0), glob (10.3.0) — Confidence: 0.95
  - Analyzer: web-tree-sitter (0.25.10), zod (4.3.6), js-yaml (4.1.1) — Confidence: 0.95
  - Testing: vitest (2.0.0 for CLI, 4.0.18 for analyzer) — Confidence: 0.95

## Directory Structure

**Monorepo layout:**

```
anatomia/
├── packages/
│   ├── cli/              # Main CLI tool (anatomia-cli)
│   │   ├── src/          # TypeScript source
│   │   ├── tests/        # Vitest tests
│   │   ├── templates/    # Template files (.md, ENTRY.md)
│   │   └── dist/         # Build output
│   ├── analyzer/         # Code analysis engine (anatomia-analyzer)
│   │   ├── src/          # TypeScript source
│   │   ├── tests/        # Vitest tests
│   │   └── dist/         # Build output
│   └── generator/        # Template generator (alpha)
│       ├── src/
│       └── dist/
├── website/              # Next.js documentation site
├── tests/                # Root-level integration tests
├── .ana/                 # Anatomia's own context (dogfooding)
├── .claude/              # Claude Code agent definitions
├── .github/workflows/    # GitHub Actions CI
└── master_plan/          # Product planning docs
```

**Source directories:**
- packages/cli/src/ (14 .ts files) — Confidence: 0.95
- packages/analyzer/src/ (71 .ts files) — Confidence: 0.95
- Organized by domain: commands/, utils/, analyzers/, parsers/, detectors/, types/

**Test directories:**
- packages/cli/tests/ — Confidence: 0.95
- packages/analyzer/tests/ — Confidence: 0.95
- Test pattern: *.test.ts files, fixtures in tests/fixtures/
- 64 total test files across packages — Confidence: 0.90

**Build outputs:**
- dist/ in each package (gitignored) — Confidence: 0.95
- .turbo/ (Turborepo cache, gitignored) — Confidence: 0.95

**Configuration locations:**
- Root: tsconfig.base.json, turbo.json, pnpm-workspace.yaml
- Per-package: tsconfig.json, vitest.config.ts, package.json
- Confidence: 0.95

**Special directories:**
- .ana/ - Project's own Anatomia context (dogfooding the CLI) — Confidence: 0.95
- .claude/ - Claude Code agent definitions (ana-explorer, ana-writer, etc.) — Confidence: 0.95
- templates/ - Template files for ana init command — Confidence: 0.95

## Patterns

### Error Handling

**Pattern:** Custom error classes with error collector pattern — Confidence: 0.85

**Evidence:**
- packages/analyzer/src/errors/DetectionError.ts: `DetectionEngineError extends Error` class
- packages/analyzer/src/errors/DetectionCollector.ts: Collector class with severity levels (errors, warnings, info)
- Graceful degradation approach: errors collected but don't halt execution
- Example from DetectionCollector.ts:
  ```typescript
  addError(error: DetectionEngineError | DetectionError): void
  hasCriticalErrors(): boolean
  getCounts() { errors, warnings, info, total }
  ```

**Pattern:** Try-catch with process.exit(1) in CLI commands — Confidence: 0.80

**Evidence:**
- packages/cli/src/index.ts: Main async wrapper with try-catch and process.exit(1)
- packages/cli/src/commands/setup.ts: Multiple try-catch blocks with chalk error output

### Validation

**Pattern:** Zod schema validation for type safety — Confidence: 0.90

**Evidence:**
- packages/analyzer/src/types/index.ts: Extensive use of Zod schemas
  - `AnalysisResultSchema`, `ProjectTypeSchema`, `ConfidenceScoreSchema`
  - Runtime validation with TypeScript type inference
- Example:
  ```typescript
  export const ConfidenceScoreSchema = z.number().min(0.0).max(1.0);
  export type ConfidenceScore = z.infer<typeof ConfidenceScoreSchema>;
  ```

**Pattern:** Multi-phase validation in setup command — Confidence: 0.85

**Evidence:**
- packages/cli/src/commands/setup.ts: 4-phase validation system
  1. Structural validation (validateStructure)
  2. Content validation (validateContent)
  3. Cross-reference validation (validateCrossReferences)
  4. Quality checks (validateQuality)
- Blocking vs warning errors distinction

### Database

**Pattern:** No database usage (CLI tool) — Confidence: 0.95

**Evidence:**
- No database dependencies in package.json files
- No database configuration files
- Pure filesystem-based operations (reading/writing .ana/ directory)

### Authentication

**Pattern:** No authentication (CLI tool) — Confidence: 0.95

**Evidence:**
- No auth libraries in dependencies
- Local-only tool, no network operations requiring auth

### Testing

**Pattern:** Vitest with comprehensive test coverage requirements — Confidence: 0.95

**Evidence:**
- vitest.config.ts in both CLI and analyzer packages
- Coverage thresholds: CLI (80% lines/functions), Analyzer (85% lines/functions)
- Test organization: tests/ directory mirrors src/ structure
- Coverage provider: v8
- Test naming: *.test.ts pattern
- 64 test files total

**Pattern:** Fixture-based testing — Confidence: 0.85

**Evidence:**
- packages/analyzer/tests/fixtures/ with language-specific subdirectories (go/, node/, python/, etc.)
- Temp directory creation in tests (mkdtemp pattern in dependencies.test.ts)

**Pattern:** Contract testing between packages — Confidence: 0.80

**Evidence:**
- packages/cli/tests/contract/analyzer-contract.test.ts
- Ensures CLI expectations match analyzer outputs

### Code Organization

**Pattern:** Domain-driven directory structure — Confidence: 0.90

**Evidence:**
- Analyzer: analyzers/, parsers/, detectors/, types/, cache/, errors/
- CLI: commands/, utils/
- Each domain is self-contained with clear boundaries

**Pattern:** Barrel exports (index.ts files) — Confidence: 0.85

**Evidence:**
- packages/analyzer/src/index.ts: Comprehensive public API export
- packages/cli/src/commands/index.ts: Command aggregation
- Controlled public surface area

## Conventions

### Naming

**Files:** kebab-case.ts — Confidence: 0.90
- Evidence: file-writer.ts, scaffold-generators.ts, format-analysis-brief.ts, analysis-helpers.ts

**Functions/Variables:** camelCase — Confidence: 0.95
- Evidence: generateProjectOverviewScaffold, detectProjectType, analyzeStructure, validateContent

**Classes:** PascalCase — Confidence: 0.95
- Evidence: DetectionCollector, DetectionEngineError, ParserManager, ASTCache, QueryCache

**Interfaces/Types:** PascalCase — Confidence: 0.95
- Evidence: AnalysisResult, ValidationError, WriteFileOptions, SymbolEntry

**Constants:** SCREAMING_SNAKE_CASE — Confidence: 0.95
- Evidence: SCAFFOLD_MARKER, MIN_FILE_SIZE_WARNING, PATTERN_CATEGORIES, REQUIRED_CONTEXT_FILES

**Enums/Literal types:** PascalCase for schema, lowercase for values — Confidence: 0.90
- Evidence: ProjectTypeSchema with values 'python', 'node', 'go', etc.

### Import Organization

**Pattern:** External imports first, internal imports second, grouped by source — Confidence: 0.90

**Evidence from packages/cli/src/commands/setup.ts:**
```typescript
import { Command } from 'commander';
import chalk from 'chalk';
import * as path from 'node:path';
import * as fs from 'node:fs/promises';
import type { AnalysisResult } from 'anatomia-analyzer';
import { validateStructure, ... } from '../utils/validators.js';
import { VALID_SETUP_TIERS, META_VERSION } from '../constants.js';
```

**Pattern:** Node built-ins use node: protocol — Confidence: 0.95
- Evidence: `import * as fs from 'node:fs/promises'`, `import * as path from 'node:path'`

**Pattern:** .js extensions in imports (ESM requirement) — Confidence: 0.95
- Evidence: All relative imports end with .js (e.g., '../utils/validators.js')

### Indentation

**Style:** 2 spaces — Confidence: 0.95
- Evidence: .prettierrc.json: `"tabWidth": 2`

**No tabs:** Spaces only — Confidence: 0.95
- Evidence: Prettier config, consistent across all files

### Type Usage

**Pattern:** Strict TypeScript with extensive checks — Confidence: 0.95

**Evidence from tsconfig.base.json:**
```json
"strict": true,
"noUncheckedIndexedAccess": true,
"noImplicitOverride": true,
"noPropertyAccessFromIndexSignature": true,
"noImplicitReturns": true,
"noFallthroughCasesInSwitch": true,
"exactOptionalPropertyTypes": true
```

**Pattern:** Explicit type annotations for public APIs — Confidence: 0.85
- Evidence: Function signatures in index.ts exports, interface definitions

**Pattern:** Type inference for internal variables — Confidence: 0.80
- Evidence: Local variables often lack explicit types, relying on inference

**Pattern:** Zod for runtime validation + type inference — Confidence: 0.90
- Evidence: z.infer<typeof Schema> pattern throughout types/

### Code Style

**Formatter:** Prettier — Confidence: 0.95
- Config: singleQuote: true, semi: true, trailingComma: 'es5', printWidth: 100

**Linter:** ESLint with TypeScript plugin — Confidence: 0.95
- Config: eslint.config.mjs, uses flat config format
- Rules: @typescript-eslint/no-explicit-any: 'warn', unused vars with underscore prefix ignore

### Documentation

**Pattern:** JSDoc comments for public APIs — Confidence: 0.85

**Evidence:**
- Extensive JSDoc in analyzer/src/index.ts
- Example annotations with @param, @returns, @example tags
- packages/cli/src/index.ts: @packageDocumentation tag

**Pattern:** Inline comments for complex logic — Confidence: 0.75
- Evidence: Comments explaining STEP phases, validation phases, edge cases

## Workflow

### Git Configuration

**.gitignore structure:** Comprehensive, categorized — Confidence: 0.95

**Evidence from .gitignore:**
```
# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/, dist/, build/, .turbo/

# Environment & Secrets
.env, *.pem, credentials.json

# IDE & OS
.DS_Store, .vscode/settings.json

# Testing
coverage/, test-results/

# Temporary
*.tmp, master_plan/, .ana/.state/cache/
```

**Git hooks:** Custom bash hooks in .ana/hooks/, NOT Husky — Confidence: 0.90
- Evidence: .ana/hooks/quality-gate.sh, run-check.sh, subagent-verify.sh
- No .husky/ directory detected
- Hooks are for Anatomia's own setup validation

### CI/CD Setup

**Platform:** GitHub Actions — Confidence: 0.95
- Evidence: .github/workflows/test.yml

**Workflow:** Multi-OS, multi-Node version testing — Confidence: 0.95

**Matrix strategy:**
- OS: ubuntu-latest, windows-latest, macos-latest
- Node: 20, 22
- fail-fast: false (run all combinations)

**Pipeline stages:**
1. Checkout code
2. Setup pnpm (v9)
3. Setup Node.js with cache
4. Install dependencies (--frozen-lockfile)
5. Build packages (pnpm build)
6. Run tests (pnpm test --run)
7. Upload coverage (Ubuntu + Node 20 only)

**Triggers:**
- Push to: main, staging, effort/** branches
- PRs to: main, staging

**Coverage:** Codecov integration — Confidence: 0.85

### Branching Strategy

**Main branch:** main — Confidence: 0.95

**Branch naming patterns:** — Confidence: 0.85
- Feature branches: effort/STEP_* (e.g., effort/STEP_1_1_DETECTION)
- Sprint branches: SideSprint/* (e.g., SideSprint/setup-redesign)
- UX branches: ux/* (e.g., ux/fix-copy)

**Evidence from git branch -a:**
- effort/STEP_0_1_FOUNDATION through effort/STEP_2_5_CLI_CODE
- SideSprint/setup-redesign
- ux/fix-copy

### Commit Conventions

**Pattern:** Structured commit messages with tags — Confidence: 0.85

**Evidence from git log:**
```
[SS-13.0] MI-51/52/53/54/55: preflight changes — Q6 git workflow, Q7 business flow
[SS-12.4c] detectProjectType tests (14), app/ directory sampling fix
[MI-18] CLAUDE.md signpost generation — marker-based merge
```

**Format:** [TAG] Brief — Details
- Tags: SS-* (SideSprint), MI-* (Milestone/Issue), STEP_* (effort tracking)
- Use of em dash (—) separator
- Descriptive details after dash

**Merge commits:** Present, indicating PR workflow — Confidence: 0.85
- Evidence: "Merge pull request #4", "Merge STEP_*" messages

### Contributors

**Team size:** Small team (2 contributors) — Confidence: 0.90
- Ryan Smith (primary)
- TettoLabs (organization account)

### Package Scripts

**Root package.json scripts:**
```json
"build": "turbo run build"
"dev": "turbo run dev"
"test": "turbo run test"
"lint": "turbo run lint"
"clean": "turbo run clean && rm -rf node_modules .turbo"
```

**CLI package scripts:**
```json
"build": "tsup && cp -r templates dist/"
"dev": "tsup --watch"
"test": "vitest"
"lint": "eslint src/"
"lint:fix": "eslint src/ --fix"
```

**Analyzer package scripts:**
```json
"build": "tsc"
"dev": "tsc --watch"
"test": "vitest"
"test:coverage": "vitest --coverage"
"clean": "rm -rf dist"
```

### Deployment

**No deployment config detected** — Confidence: 0.90
- No Dockerfile, docker-compose.yml, k8s configs
- No serverless configs
- CLI tool distributed via npm (anatomia-cli package)

### Automation

**Turborepo caching:** Enabled — Confidence: 0.95
- Task dependencies: build depends on ^build (upstream packages)
- Cache outputs: dist/**, coverage/**, .next/**
- Global dependencies tracked: tsconfig.base.json, pnpm-lock.yaml

## Testing

### Test Framework

**Framework:** Vitest — Confidence: 0.95
- Version: 2.0.0 (CLI), 4.0.18 (analyzer)
- Node environment
- globals: true (no need to import describe/it/expect)

### Test Configuration

**CLI coverage thresholds:**
- lines: 80%
- branches: 75%
- functions: 80%
- statements: 80%

**Analyzer coverage thresholds:**
- lines: 85%
- branches: 80%
- functions: 85%
- statements: 85%

**Coverage reporter:** v8 provider, outputs: text, json, html

**Exclusions:**
- dist/**
- **/*.test.ts
- src/index.ts (entry points)

### Test Structure

**Directory structure:** tests/ mirrors src/ — Confidence: 0.90

**Naming convention:** *.test.ts — Confidence: 0.95

**Test categories:**
- Unit tests: analyzers/, parsers/, detectors/
- Integration tests: tests/integration/
- Contract tests: tests/contract/analyzer-contract.test.ts
- Scaffold tests: tests/scaffolds/
- Performance tests: tests/performance/

**Fixture organization:** tests/fixtures/{language}/ — Confidence: 0.85
- Subdirectories: go/, node/, python/, ruby/, rust/, php/

### Test Patterns

**Pattern:** describe/it nesting with clear descriptions — Confidence: 0.90

**Evidence from all-scaffolds.test.ts:**
```typescript
describe('all scaffolds integration', () => {
  it('all 7 generators produce valid scaffolds', () => { ... })
  it('section count validation', () => { ... })
})
```

**Pattern:** beforeEach/afterEach for setup/teardown — Confidence: 0.85
- Evidence: dependencies.test.ts uses beforeEach for mkdtemp, afterEach for cleanup

**Pattern:** Temp directory cleanup — Confidence: 0.85
```typescript
beforeEach(async () => {
  testDir = await mkdtemp(join(tmpdir(), 'pattern-test-'));
});
afterEach(async () => {
  await rm(testDir, { recursive: true, force: true });
});
```

## Debugging

### Logging

**CLI logging:** chalk for colored output — Confidence: 0.95

**Evidence:**
- chalk.blue(), chalk.red(), chalk.green(), chalk.gray(), chalk.yellow()
- User-facing messages in commands
- No structured logging library

**Pattern:** Console.log/error for output — Confidence: 0.90
- Direct console output, not abstracted logger
- Appropriate for CLI tool

### Error Handling

**Pattern:** Graceful degradation with error collection — Confidence: 0.85

**Evidence:**
- DetectionCollector class accumulates errors without halting
- strictMode option for failing fast vs continuing
- Example from analyzer/src/index.ts:
  ```typescript
  catch (error) {
    if (options.strictMode) { throw error; }
    return createEmptyAnalysisResult();
  }
  ```

**Pattern:** User-friendly CLI error messages — Confidence: 0.85
- Colored output with chalk
- Actionable suggestions (e.g., "Run `ana init` first")
- Examples from setup.ts

### Observability

**No APM/monitoring detected** — Confidence: 0.95
- No Sentry, Datadog, New Relic dependencies
- CLI tool with local execution, no remote telemetry

### Debug Configuration

**No .vscode/launch.json detected** — Confidence: 0.80
- .vscode/settings.json is gitignored
- Developers likely use tsx for running TypeScript directly

## Project Maturity Signals

### Codebase Size

- **Total TypeScript lines:** ~29,339 (packages only) — Confidence: 0.90
- **Source files:** 71 .ts files (src/ only) — Confidence: 0.90
- **Test files:** 64 .test.ts files — Confidence: 0.90
- **Documentation lines:** ~340 (READMEs) — Confidence: 0.85

### Git History

**Commit depth:** 20+ recent commits visible — Confidence: 0.85
- Active development with consistent commit pattern
- Recent work on setup redesign (SS-* tags)

**Branch count:** 13+ effort/* branches, active SideSprint branch — Confidence: 0.85
- Organized by development phases (STEP_0_* through STEP_2_*)
- Methodical, phased approach to development

### Documentation

**README completeness:** High — Confidence: 0.90
- Root README: Project overview, structure, dev setup
- Package READMEs: CLI and analyzer documentation
- CONTRIBUTING.md with detailed setup instructions
- Inline JSDoc for public APIs

**Special docs:**
- master_plan/ directory with vision and implementation docs
- .ana/ directory (dogfooding own product)
- .claude/ agent definitions

### Testing Maturity

**Coverage requirements:** High (80-85% thresholds) — Confidence: 0.95
- Enforced via vitest.config.ts
- Multi-platform CI testing (Windows, macOS, Linux)

**Test variety:** Unit, integration, contract, performance — Confidence: 0.85

### CI/CD Maturity

**Multi-OS testing:** 3 operating systems — Confidence: 0.95

**Multi-version testing:** Node 20 and 22 — Confidence: 0.95

**Frozen lockfile:** Enforced via --frozen-lockfile — Confidence: 0.95

**Coverage tracking:** Codecov integration — Confidence: 0.85

### Monorepo Maturity

**Turborepo setup:** Full configuration with caching — Confidence: 0.95

**Workspace management:** pnpm workspaces with 3 packages — Confidence: 0.95

**Shared config:** tsconfig.base.json, centralized tooling — Confidence: 0.95

## Development Workflow

### Recent Commit History

**Pattern:** Feature-tagged commits with detailed descriptions — Confidence: 0.85

**Last 20 commits:**
1. [SS-13.0] Preflight changes for MI-51-55
2. [SS-12.4c] detectProjectType tests + app/ sampling
3. [SS-12.4b] Fix analysis.md field names
4. [SS-12.4] Analyzer detectProjectType() implementation
5. [MI-18] CLAUDE.md signpost generation
6. [SS-12.3] Easy wins (duplicate H2, P0 extraction)
7. [SS-12.2] Systematic path fixes
8. [SS-11.1] Fix init atomic rename
9. [SS-11] AST symbol index (582 symbols from 152 files)
10. [SS-10.1] Add WASM smoke test

**Observation:** Commits are granular, well-documented, with clear scope

### Branch Structure

**Active branches:** 13 effort/* branches + 1 SideSprint branch — Confidence: 0.85

**Naming convention:**
- effort/STEP_{phase}_{checkpoint}_{name}
- SideSprint/{feature}
- ux/{feature}

**Merge pattern:** PR-based workflow with merge commits — Confidence: 0.85
- Evidence: "Merge pull request #4", "Merge STEP_*" commits

### Contributor Activity

**Primary contributor:** Ryan Smith — Confidence: 0.90
**Organization:** TettoLabs — Confidence: 0.90
**Team size:** Solo/small team (2 identities) — Confidence: 0.85

### Git Hooks

**Custom hooks in .ana/hooks/:** — Confidence: 0.90
- quality-gate.sh: Blocks session completion if context files fail validation
- run-check.sh: Wrapper for validation checks
- subagent-verify.sh: Sub-agent verification
- verify-context-file.sh: Individual file verification

**No standard pre-commit tools detected** (no Husky, lint-staged) — Confidence: 0.90

### Development Tools

**Local dev:** tsx for running TypeScript — Confidence: 0.80
- Evidence: tsx in CLI devDependencies
- No ts-node detected

**Linking:** npm link for local testing — Confidence: 0.80
- Evidence: CONTRIBUTING.md mentions "npm link"

---

*This exploration scanned 85+ files, examined 29k+ lines of TypeScript, and analyzed git history, CI/CD configuration, and project structure. All findings include evidence and confidence scores.*
