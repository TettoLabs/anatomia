schema: 1
concerns:
  - summary: "Worktree metadata (used, created_at, completed_at, commit_count) not written to proof chain entry — ProofChainEntry type lacks worktree field"
    severity: debt
    suggested_action: scope
  - summary: "Phase detection in startWork uses globSync for numbered specs which could be slow with many plans"
    severity: observation
    suggested_action: monitor
  - summary: "process.exit in startWork makes unit testing phase detection difficult — throw-based refactor would improve testability"
    severity: debt
    suggested_action: monitor
  - summary: "_branchPrefix parameter kept in getNextAction for API compat but unused"
    severity: observation
    suggested_action: accept
  - summary: "Pre-existing lint warning in git-operations.ts:169 — unused eslint-disable directive"
    file: "packages/cli/src/utils/git-operations.ts"
    severity: observation
    suggested_action: accept
